export const defaultPaging = {
    take: 50 // 10 is small enough to show all the records due the page? //previous value is 10
}
export const defaultPagingList = {
    take: [5, 10, 20, 50, 200, 500, 1000] 
}